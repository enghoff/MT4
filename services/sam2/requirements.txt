torch
# Sam2Model / Sam2Processor arrived in transformers 4.57; 5.14.1 is what the
# reference deployment runs.
transformers>=4.57
fastapi
uvicorn[standard]
pillow
python-multipart
numpy

# Not imported by server.py: transformers reaches for torchvision on the image
# processor's fast path (the one `device="cuda"` preprocessing needs) and for
# accelerate inside from_pretrained.
torchvision
accelerate
